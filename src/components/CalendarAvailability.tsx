// Sincronización con Google Calendar desactivada temporalmente hasta que se digitalice la información de reservas.
export function CalendarAvailability(_props?: { checkIn?: string; checkOut?: string }) {
  return null;
}
